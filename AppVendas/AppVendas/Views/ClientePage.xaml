<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:components="clr-namespace:AppVendas.Views.Controls"
             mc:Ignorable="d"
             x:Class="AppVendas.Views.ClientePage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <components:LabelSemInternet/>
            <StackLayout Padding="{StaticResource PaddingTela}">
                <Label Text="{Binding RazaoSocial}" FontSize="Large" FontAttributes="Bold"/>
                <Label Text="{Binding RazaoSocial}"/>
                <Label Text="{Binding CidadeEstado}"/>
                <Label Text="{Binding DataUltimoPedido}"/>
                <Button Text="Novo pedido" Clicked="BtnNovoPedido_Clicked"/>
                <Button Text="Pedidos anteriores" Clicked="BtnPedidosAnteriores_Clicked"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>