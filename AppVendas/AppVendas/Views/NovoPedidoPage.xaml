<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppVendas.Views.NovoPedidoPage">
    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding Produtos}" EmptyView="Nenhum produto encontrado para esse cliente">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="{StaticResource PaddingGrid}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.RowSpan="3" Source="sem_foto.png"  />
                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding Descricao}"/>
                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding Unidade}"/>
                        <Label Grid.Column="1" Grid.Row="2" Text="{Binding ValorUnitario}"/>
                        <Entry Grid.Column="2" Grid.Row="0" Text="{Binding Quantidade, Mode=TwoWay}" Keyboard="Numeric"/>
                        <Label Grid.Column="2" Grid.Row="1" Text="{Binding ValorTotal, Mode=OneWay}"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>