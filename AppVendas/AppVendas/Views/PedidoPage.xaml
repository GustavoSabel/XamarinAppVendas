<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppVendas.Views.PedidoPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout Padding="{StaticResource PaddingTela}">
            <Label Text="{Binding ValorTotal, StringFormat='Total: {0:c2}'}" FontSize="Large" FontAttributes="Bold"/>
            <Label Text="{Binding Data}" FontSize="Medium" Margin="0, 0, 0, 20"/>

            <CollectionView ItemsSource="{Binding Produtos}" EmptyView="Nada" SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Row="0" Grid.ColumnSpan="3" Text="{Binding Descricao}" />
                            <Label Grid.Row="1" Grid.Column="0" Margin="0, 0, 10, 0" Text="{Binding Quantidade, StringFormat='Qtd: {0}'}" />
                            <Label Grid.Row="1" Grid.Column="1" Margin="0, 0, 10, 0" Text="{Binding Valor, StringFormat='Valor: {0:c2}'}" />
                            <Label Grid.Row="1" Grid.Column="2" Text="{Binding ValorUnitario, StringFormat='Valor Unit.: {0:c2}'}" />
                            <Label Grid.Row="2" Grid.ColumnSpan="3" Text="{Binding ValorTotal,StringFormat='Total: {0:c2}'}" FontAttributes="Bold" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>